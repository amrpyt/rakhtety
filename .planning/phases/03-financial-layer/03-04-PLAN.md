---
phase: 03
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/hooks/useFinancialDashboard.ts
  - src/components/dashboard/FinancialKpiGrid.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements:
  - FIN-05

must_haves:
  truths:
    - "D-09: Dashboard shows total fees collected, realized office profit, and total outstanding debt"
    - "D-10: Dashboard reuses existing KpiCard and KpiGrid patterns"
  artifacts:
    - path: "src/hooks/useFinancialDashboard.ts"
      provides: "Dashboard financial aggregate hook"
      contains: "useFinancialDashboard"
    - path: "src/components/dashboard/FinancialKpiGrid.tsx"
      provides: "Financial KPI cards"
      contains: "realized_profit"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard page integration"
      contains: "FinancialKpiGrid"
  key_links:
    - from: "dashboard page"
      to: "financial service"
      via: "useFinancialDashboard hook"
---

<objective>
Add financial KPI widgets to the dashboard.

Purpose: Managers need the three money numbers at a glance: collected fees, realized profit, and outstanding debt.
Output: Dashboard hook, financial KPI grid, and dashboard page integration.
</objective>

<execution_context>
@D:/Vibe Coding/02-Real PRojects with Clints/رخصتي/.codex/get-shit-done/workflows/execute-plan.md
@D:/Vibe Coding/02-Real PRojects with Clints/رخصتي/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-financial-layer/03-CONTEXT.md
@src/components/dashboard/KpiCard.tsx
@src/components/dashboard/KpiGrid.tsx
@src/app/(dashboard)/dashboard/page.tsx
@src/lib/services/financial.service.ts

## Dashboard numbers

- Total fees collected
- Realized office profit
- Total outstanding debt

Use EGP formatting and Arabic labels.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard finance hook</name>
  <files>src/hooks/useFinancialDashboard.ts</files>
  <read_first>
    - src/lib/services/financial.service.ts
    - src/hooks/useFinancials.ts
  </read_first>
  <action>
Create `src/hooks/useFinancialDashboard.ts`.

The hook should:

- call `financialService.calculateDashboardSummary`
- expose `summary`
- expose `loading`
- expose `error`
- expose `refresh`

Follow the style of existing hooks.
</action>
  <verify>
    <automated>Select-String -Path src/hooks/useFinancialDashboard.ts -Pattern "useFinancialDashboard"</automated>
  </verify>
  <done>
    Dashboard can load financial aggregates.
  </done>
  <acceptance_criteria>
    - Hook exports `useFinancialDashboard`
    - Hook loads dashboard summary
    - Hook handles loading and error state
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create financial KPI grid</name>
  <files>src/components/dashboard/FinancialKpiGrid.tsx</files>
  <read_first>
    - src/components/dashboard/KpiCard.tsx
    - src/components/dashboard/KpiGrid.tsx
    - src/types/database.types.ts
  </read_first>
  <action>
Create `src/components/dashboard/FinancialKpiGrid.tsx`.

Use existing `KpiCard` or `KpiGrid`.

Cards:

- `إجمالي الرسوم المحصلة`
- `ربح المكتب المحصل`
- `المديونية المتبقية`

Format values as EGP.
</action>
  <verify>
    <automated>Select-String -Path src/components/dashboard/FinancialKpiGrid.tsx -Pattern "المديونية"</automated>
  </verify>
  <done>
    Financial KPI cards are ready for dashboard integration.
  </done>
  <acceptance_criteria>
    - Component exports `FinancialKpiGrid`
    - Shows three required cards
    - Uses existing dashboard card styling
    - Formats currency as EGP
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate financial KPIs into dashboard page</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/page.tsx
    - src/components/dashboard/FinancialKpiGrid.tsx
    - src/hooks/useFinancialDashboard.ts
  </read_first>
  <action>
Update dashboard page to render `FinancialKpiGrid`.

Keep existing dashboard cards. Add financial cards in a clear section, preferably near other KPI cards.

Do not remove existing dashboard content.
</action>
  <verify>
    <automated>Select-String -Path "src/app/(dashboard)/dashboard/page.tsx" -Pattern "FinancialKpiGrid"</automated>
  </verify>
  <done>
    Dashboard shows Phase 3 financial metrics.
  </done>
  <acceptance_criteria>
    - Dashboard imports `FinancialKpiGrid`
    - Dashboard renders financial KPI section
    - Existing dashboard content still exists
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard-to-service | Dashboard requests aggregate financial data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Information Disclosure | financial dashboard | mitigate | Aggregates use same Supabase RLS-backed data access |
| T-03-10 | Integrity | dashboard summary | mitigate | Values come from financial service source-of-truth calculations |
</threat_model>

<verification>
- Run `pnpm typecheck`
- Open dashboard and verify financial cards render
- Check Arabic labels and EGP formatting
</verification>

<success_criteria>
1. Dashboard shows total fees collected.
2. Dashboard shows realized office profit.
3. Dashboard shows outstanding debt.
4. Cards reuse existing dashboard visual style.

<output>
After completion, create `.planning/phases/03-financial-layer/03-04-SUMMARY.md`
</output>
