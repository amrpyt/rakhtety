---
phase: 03
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/hooks/useFinancials.ts
  - src/components/financial/PaymentForm.tsx
  - src/components/financial/FinancialSummaryCard.tsx
  - src/app/(dashboard)/clients/[id]/page.tsx
autonomous: true
requirements:
  - FIN-02
  - FIN-03
  - FIN-04

must_haves:
  truths:
    - "D-01: Users can record workflow-level or step-specific payments"
    - "D-03: UI shows total cost, total paid, and outstanding debt from service calculations"
    - "D-10: UI remains Arabic-friendly and reuses existing design patterns"
  artifacts:
    - path: "src/hooks/useFinancials.ts"
      provides: "React hook bridge between UI and financial service"
      contains: "useFinancials"
    - path: "src/components/financial/PaymentForm.tsx"
      provides: "Payment recording form"
      contains: "recordPayment"
    - path: "src/components/financial/FinancialSummaryCard.tsx"
      provides: "Workflow/client financial summary display"
      contains: "outstanding_debt"
  key_links:
    - from: "client profile page"
      to: "financial service"
      via: "useFinancials hook"
---

<objective>
Add payment recording and financial summary UI to the client workflow area.

Purpose: Users need to enter money received and immediately see what is paid and what is still owed.
Output: Hook, payment form, summary card, and client page integration.
</objective>

<execution_context>
@D:/Vibe Coding/02-Real PRojects with Clints/رخصتي/.codex/get-shit-done/workflows/execute-plan.md
@D:/Vibe Coding/02-Real PRojects with Clints/رخصتي/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-financial-layer/03-CONTEXT.md
@src/hooks/useWorkflows.ts
@src/components/ui/Form.tsx
@src/components/ui/Button.tsx
@src/components/ui/Card.tsx
@src/components/workflow/WorkflowTabs.tsx
@src/app/(dashboard)/clients/[id]/page.tsx

## UX requirement

Keep it simple:

- show total cost
- show total paid
- show remaining debt
- let user record a payment

No invoice/VAT/general ledger in MVP.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useFinancials hook</name>
  <files>src/hooks/useFinancials.ts</files>
  <read_first>
    - src/hooks/useWorkflows.ts
    - src/lib/services/financial.service.ts
  </read_first>
  <action>
Create `src/hooks/useFinancials.ts`.

The hook should:

- accept `workflowId` and optional `clientId`
- load workflow financial summary
- expose `recordPayment`
- expose `refresh`
- expose `loading` and `error`

Follow the style of `useWorkflows.ts`.
</action>
  <verify>
    <automated>Select-String -Path src/hooks/useFinancials.ts -Pattern "useFinancials"</automated>
  </verify>
  <done>
    UI has a hook for financial summaries and payment recording.
  </done>
  <acceptance_criteria>
    - Hook exports `useFinancials`
    - Hook exposes `summary`
    - Hook exposes `recordPayment`
    - Hook handles loading and error state
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create payment form component</name>
  <files>src/components/financial/PaymentForm.tsx</files>
  <read_first>
    - src/components/ui/Form.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Card.tsx
  </read_first>
  <action>
Create `src/components/financial/PaymentForm.tsx`.

Fields:

- amount
- payment method
- optional reference number
- optional notes
- optional workflow step selector if steps are passed in

Behavior:

- validate amount > 0 in the component before submit
- call `onSubmit`
- clear form after success
- show Arabic labels

Do not put `dir="rtl"` on this component. RTL is handled at the app root.
</action>
  <verify>
    <automated>Select-String -Path src/components/financial/PaymentForm.tsx -Pattern "amount"</automated>
  </verify>
  <done>
    Payment form can collect payment input safely.
  </done>
  <acceptance_criteria>
    - Component exports `PaymentForm`
    - Amount field is required
    - Amount must be positive
    - Component supports optional step selection
    - Arabic labels are used
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create financial summary card</name>
  <files>src/components/financial/FinancialSummaryCard.tsx</files>
  <read_first>
    - src/components/ui/Card.tsx
    - src/types/database.types.ts
  </read_first>
  <action>
Create `src/components/financial/FinancialSummaryCard.tsx`.

Display:

- total cost
- total paid
- outstanding debt
- planned profit
- realized profit

Format values as EGP. Keep styling close to existing cards.
</action>
  <verify>
    <automated>Select-String -Path src/components/financial/FinancialSummaryCard.tsx -Pattern "outstanding"</automated>
  </verify>
  <done>
    User can see the money summary for a workflow/client.
  </done>
  <acceptance_criteria>
    - Component exports `FinancialSummaryCard`
    - Shows total cost
    - Shows total paid
    - Shows outstanding debt
    - Shows planned and realized profit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Integrate finance UI into client profile page</name>
  <files>src/app/(dashboard)/clients/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/clients/[id]/page.tsx
    - src/components/financial/PaymentForm.tsx
    - src/components/financial/FinancialSummaryCard.tsx
    - src/hooks/useFinancials.ts
  </read_first>
  <action>
Update the client profile page to show financial summary and payment form near the workflow details.

Use the selected workflow when possible. If both Device License and Excavation Permit exist, show summaries per workflow or pick the active workflow tab pattern already present.

Keep scope tight:

- No invoices
- No VAT
- No PDF receipts
</action>
  <verify>
    <automated>Select-String -Path "src/app/(dashboard)/clients/[id]/page.tsx" -Pattern "PaymentForm|FinancialSummaryCard"</automated>
  </verify>
  <done>
    Client page supports recording payments and viewing debt.
  </done>
  <acceptance_criteria>
    - Client page imports financial components
    - User can submit a payment
    - Summary refreshes after payment
    - Existing workflow UI still renders
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-service | User-entered amount/reference/notes leave the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tamper | PaymentForm.amount | mitigate | Component validates positive amount; service/database validate again |
| T-03-08 | Information Disclosure | client page financial section | mitigate | Uses existing auth and Supabase RLS-backed service calls |
</threat_model>

<verification>
- Run `pnpm typecheck`
- Manually verify client profile still loads
- Verify payment form rejects zero/negative amount
</verification>

<success_criteria>
1. User can record a payment against a workflow.
2. User can optionally allocate a payment to a step.
3. User can see total cost, total paid, and outstanding debt.
4. Existing client/workflow page behavior remains intact.

<output>
After completion, create `.planning/phases/03-financial-layer/03-03-SUMMARY.md`
</output>
