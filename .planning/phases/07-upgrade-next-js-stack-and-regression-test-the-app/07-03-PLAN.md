---
phase: 07-upgrade-next-js-stack-and-regression-test-the-app
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - open-next.config.ts
  - wrangler.jsonc
  - package.json
  - pnpm-lock.yaml
  - cloudflare-env.d.ts
  - .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-03-SUMMARY.md
autonomous: true
requirements:
  - P7-01
  - P7-06
must_haves:
  truths:
    - "The app remains locally production-buildable before Cloudflare checks."
    - "OpenNext Cloudflare preview is attempted after a green Next build."
    - "Any provider/tool blocker is documented with exact command output."
  artifacts:
    - path: "open-next.config.ts"
      provides: "OpenNext Cloudflare configuration"
    - path: "wrangler.jsonc"
      provides: "Cloudflare Worker, assets, env, and R2 binding config"
    - path: ".planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-03-SUMMARY.md"
      provides: "Cloudflare readiness result and blocker notes"
  key_links:
    - from: "package.json"
      to: "open-next.config.ts"
      via: "pnpm preview script"
      pattern: "opennextjs-cloudflare preview"
    - from: "wrangler.jsonc"
      to: "OpenNext output"
      via: ".open-next/worker.js and assets binding"
      pattern: ".open-next"
---

<objective>
Verify Cloudflare/OpenNext deployment readiness as far as local tooling allows.

Purpose: Phase 6 was blocked by deployment compatibility. This plan proves the upgraded Next.js stack can be packaged for Cloudflare, or records the exact remaining external blocker.
Output: Green local build plus OpenNext preview evidence or a precise deployment blocker.
</objective>

<scope>
Allowed: Cloudflare/OpenNext config fixes, Wrangler type generation, patch-level Wrangler/OpenNext dependency update only when required by a real preview error, and documentation of missing credentials or provider resources.

Not allowed: app feature rewrites, replacing OpenNext with another adapter, adding a new hosting platform, or making Phase 8 Frappe changes.
</scope>

<context>
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-02-SUMMARY.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-RESEARCH.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-PATTERNS.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Confirm local production gates before Cloudflare packaging</name>
  <files>.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-03-SUMMARY.md</files>
  <action>Run the full local gate first. Do not debug Cloudflare until normal Next build is green. Record any middleware/proxy warning from build output because the user called it out as current baseline.</action>
  <verify>
    <automated>pnpm lint</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>Local production build passes before OpenNext work starts.</done>
</task>

<task type="auto">
  <name>Task 2: Run OpenNext Cloudflare preview path</name>
  <files>open-next.config.ts, wrangler.jsonc, cloudflare-env.d.ts, .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-03-SUMMARY.md</files>
  <action>Run `pnpm preview`. If it fails, classify the failure: local config, Windows path/tool issue, missing Cloudflare credentials, missing R2 bucket, missing env secret, or provider incompatibility. Fix local config/tool issues inside this plan. For external account/resource blockers, document exact command, error, and required human action per D-10 and D-11.</action>
  <verify>
    <automated>pnpm preview</automated>
  </verify>
  <done>`pnpm preview` either serves locally or the summary contains a precise blocker that cannot be solved from this repo.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve Cloudflare secret and RLS expectations</name>
  <files>wrangler.jsonc, cloudflare-env.d.ts, .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-03-SUMMARY.md</files>
  <action>Check that public Supabase values remain `NEXT_PUBLIC_*` vars and service-role secrets are not committed into config. If Wrangler type generation is needed, run `pnpm cf-typegen`. Do not add database migrations unless a real RLS issue is found; if adding RLS policies, every policy must include `TO authenticated`.</action>
  <verify>
    <automated>pnpm cf-typegen</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>No secrets are committed, Cloudflare env expectations are clear, and all local gates remain green before commit.</done>
</task>
</tasks>

<risk_notes>
- Cloudflare preview can fail for reasons outside repo control, like credentials or missing R2 bucket.
- Patch dependency bumps may touch `pnpm-lock.yaml`; do them only when a real tool error justifies it.
- Cloudflare runtime can expose auth/session differences that normal `next build` will not catch. Browser checks still run in Plan 04.
</risk_notes>

<acceptance_criteria>
- Full local gate is green before Cloudflare checks.
- `pnpm preview` result is recorded.
- If blocked, the blocker names the exact command, error, and whether the next action is repo fix or human Cloudflare setup.
- No secrets are added to tracked files.
</acceptance_criteria>

