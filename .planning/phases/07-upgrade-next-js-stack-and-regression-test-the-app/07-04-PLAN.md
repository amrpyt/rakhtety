---
phase: 07-upgrade-next-js-stack-and-regression-test-the-app
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-VERIFICATION.md
  - .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-04-SUMMARY.md
autonomous: false
requirements:
  - P7-02
  - P7-03
  - P7-04
  - P7-05
  - P7-07
must_haves:
  truths:
    - "Admin can log in and use dashboard, clients, workflows, finance/reporting, and employee management."
    - "Employee can log in, see assigned work, and cannot access admin-only controls."
    - "Excavation Permit remains blocked until Device License is completed."
    - "Arabic RTL layout remains active in the browser."
  artifacts:
    - path: ".planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-VERIFICATION.md"
      provides: "Browser Use regression evidence"
    - path: ".planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-04-SUMMARY.md"
      provides: "Final Phase 7 execution summary"
  key_links:
    - from: "Browser Use"
      to: "http://localhost:3000"
      via: "real admin and employee sessions"
      pattern: "localhost:3000"
    - from: "client detail workflow UI"
      to: "WorkflowService.checkDependency"
      via: "Excavation locked before Device License completion"
      pattern: "Device License"
---

<objective>
Run final Browser Use regression checks and record production-readiness proof.

Purpose: Build checks prove the code compiles. Browser Use proves the office app still works like a real user expects, including Arabic RTL and role gates.
Output: Phase 7 verification file with admin and employee browser evidence.
</objective>

<scope>
Allowed: documentation of verification results and tiny fixes only if Browser Use reveals a blocker. Any source fix must trigger the full checks before commit and must preserve D-03, D-06, D-08, and D-09.

Not allowed: using Playwright instead of Browser Use for E2E, skipping employee checks, changing business rules to make the flow pass, or leaving an unverified source edit.
</scope>

<context>
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-03-SUMMARY.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-CONTEXT.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-PATTERNS.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Start stable local app and rerun gates</name>
  <files>.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-VERIFICATION.md</files>
  <action>Run final static gates, then start the app on `http://localhost:3000` if available because auth handoff worked better there in prior notes. If 3000 is busy, use the next free port and record the port. Keep the server running only for Browser Use checks.</action>
  <verify>
    <automated>pnpm lint</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>Local app is running for Browser Use and all static gates are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser Use admin and employee regression</name>
  <what-built>The upgraded Next.js app is locally running with green automated gates.</what-built>
  <how-to-verify>
Use Browser Use, not Playwright. Verify:
1. Admin login with the admin test account from `AGENTS.md` reaches `/dashboard`.
2. Dashboard KPIs render without auth bounce.
3. Clients list, client detail, and required intake document UI render.
4. Workflows queue renders.
5. Finance/reporting route renders and an existing client report can be opened or generated.
6. Employee management is visible to admin.
7. Employee login with the employee test account reaches `/dashboard`.
8. Employee sees assigned workflow work but cannot access employee management or admin-only actions.
9. Excavation Permit is blocked before Device License completion and unlocks only after Device License is complete.
10. Browser DOM has Arabic root direction: `document.documentElement.dir === "rtl"`.
Record pass/fail notes in `07-VERIFICATION.md`.
  </how-to-verify>
  <resume-signal>Type "approved" when Browser Use checks pass, or list exact failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize Phase 7 verification and commit</name>
  <files>.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-VERIFICATION.md, .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-04-SUMMARY.md</files>
  <action>Create the final verification table with results for lint, tests, typecheck, build, Cloudflare preview, Browser Use admin, Browser Use employee, role gates, Device-License-before-Excavation, finance/reporting, and RTL. If Browser Use found source issues and they were fixed, rerun all gates before committing. This implements D-02, D-03, D-07, D-08, and D-09.</action>
  <verify>
    <automated>pnpm lint</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>Verification file is complete, all automated gates are green, Browser Use evidence is recorded, and only Phase 7 files plus intentional source fixes are committed.</done>
</task>
</tasks>

<risk_notes>
- Browser auth may bounce to `/login` if storage/cookies are stale. Check browser storage and app session parsing before blaming credentials.
- Browser Use file inputs may need careful handling; document any workaround.
- This plan is intentionally not fully autonomous because real browser verification must be reviewed.
</risk_notes>

<acceptance_criteria>
- `07-VERIFICATION.md` includes automated gate results and Browser Use results.
- Admin and employee flows are both checked.
- Role gates, RTL, workflow dependency, finance/reporting, and dashboard/client/workflow routes are proven or exact blockers are listed.
- Final commit happens only after green automated checks.
</acceptance_criteria>

