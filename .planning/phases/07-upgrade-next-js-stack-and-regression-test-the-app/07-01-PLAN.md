---
phase: 07-upgrade-next-js-stack-and-regression-test-the-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next-env.d.ts
  - .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-01-SUMMARY.md
autonomous: true
requirements:
  - P7-01
  - P7-07
must_haves:
  truths:
    - "The current Next.js app baseline is known before compatibility edits begin."
    - "Lint, tests, typecheck, and build have clear pass/fail evidence."
    - "No user-owned work is reverted while clearing generated build state."
  artifacts:
    - path: ".planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-01-SUMMARY.md"
      provides: "Baseline command results and exact blockers"
    - path: "package.json"
      provides: "Stable production gate scripts"
      contains: "lint"
    - path: "next-env.d.ts"
      provides: "Next generated type environment"
  key_links:
    - from: "package.json"
      to: "Phase 7 checks"
      via: "scripts lint/test/typecheck/build"
      pattern: "\"typecheck\".*next typegen"
---

<objective>
Establish the Phase 7 baseline and remove only generated build-state blockers, especially stale `.next/lock`.

Purpose: This is the clean starting line. The executor proves what is already green and what is actually broken before touching app behavior.
Output: A committed baseline summary with command results and any exact blocker.
</objective>

<scope>
Allowed: inspect running Node/Next processes, remove generated `.next` build output only when it is confirmed stale, regenerate Next type output, and update dependency lockfiles only if `pnpm install` is required to restore consistency.

Not allowed: app source behavior edits, workflow rule changes, role gate changes, RLS policy changes, UI redesign, or Phase 8 Frappe work.
</scope>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-CONTEXT.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-RESEARCH.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-PATTERNS.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Capture dirty-worktree and tool baseline</name>
  <files>.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-01-SUMMARY.md</files>
  <action>Record `git status --short`, `node --version`, `pnpm --version`, and the relevant `package.json` scripts. Treat all pre-existing modified/untracked app files as user-owned. Do not revert them.</action>
  <verify>
    <automated>git status --short</automated>
    <automated>node --version; pnpm --version</automated>
  </verify>
  <done>Summary identifies pre-existing dirty files and confirms the executor will only commit files touched by this plan.</done>
</task>

<task type="auto">
  <name>Task 2: Clear stale build lock safely</name>
  <files>next-env.d.ts</files>
  <action>If `pnpm build` fails immediately because another Next build is running or `.next/lock` exists, confirm no repo-owned `next build` process is active, then remove only generated `.next` build output or lock state. Keep user dev servers alive unless they are the confirmed blocker. This implements D-01, D-02, D-04, D-05, and D-07.</action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>`pnpm build` reaches real compilation. If it still fails, the summary includes the exact command and error, not just "build failed".</done>
</task>

<task type="auto">
  <name>Task 3: Run production baseline gates</name>
  <files>.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-01-SUMMARY.md</files>
  <action>Run the current gates in order: `pnpm lint`, `pnpm test`, `pnpm typecheck`, `pnpm build`. Record warnings separately from failures. Current expected baseline from research: lint exits 0 with warnings, tests/typecheck pass, and build should pass after stale lock cleanup or expose a real compatibility error.</action>
  <verify>
    <automated>pnpm lint</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>All four command results are recorded. Commit only this plan's changed files when the requested checks are green; if not green, fix within scope or leave a precise blocker for Plan 02.</done>
</task>
</tasks>

<risk_notes>
- Build lock cleanup can accidentally remove useful generated diagnostics. Only generated `.next` output may be removed.
- The repo is already dirty. Do not include unrelated app changes in this plan commit.
- Middleware/proxy convention warning is expected research context; this plan only captures it unless it blocks build.
</risk_notes>

<acceptance_criteria>
- Baseline summary exists.
- `pnpm lint`, `pnpm test`, `pnpm typecheck`, and `pnpm build` have fresh results.
- Any remaining blocker is exact enough for the next executor to fix without guessing.
- No app source behavior was changed in this plan.
</acceptance_criteria>

