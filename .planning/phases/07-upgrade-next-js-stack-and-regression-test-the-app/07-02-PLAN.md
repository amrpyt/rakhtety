---
phase: 07-upgrade-next-js-stack-and-regression-test-the-app
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/middleware.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/proxy.ts
  - src/providers/AuthProvider.tsx
  - src/hooks/useClients.ts
  - src/hooks/useDashboardAnalytics.ts
  - src/hooks/useDocuments.ts
  - src/hooks/useEmployees.ts
  - src/hooks/useFinancialDashboard.ts
  - src/hooks/useFinancials.ts
  - src/hooks/useWorkflows.ts
  - src/**/*.test.ts
  - .planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-02-SUMMARY.md
autonomous: true
requirements:
  - P7-01
  - P7-02
  - P7-04
  - P7-05
must_haves:
  truths:
    - "Next.js 16 and React 19 compatibility issues are fixed with small local changes."
    - "Auth session handling still preserves admin and employee access boundaries."
    - "Device License must still complete before Excavation Permit can start."
  artifacts:
    - path: "src/middleware.ts"
      provides: "Protected route and role redirect guard"
      exports: ["middleware"]
    - path: "src/lib/supabase/server.ts"
      provides: "Async Next cookies Supabase server helper"
      exports: ["createServerClient"]
    - path: "src/hooks/useWorkflows.ts"
      provides: "Workflow UI data bridge"
    - path: "src/lib/services/workflow.service.test.ts"
      provides: "Workflow dependency regression tests"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/proxy.ts"
      via: "updateSession(request)"
      pattern: "updateSession"
    - from: "src/hooks/useWorkflows.ts"
      to: "src/lib/services/workflow.service.ts"
      via: "workflow actions keep dependency rule"
      pattern: "EXCAVATION_PERMIT"
---

<objective>
Fix actual Next.js 16 / React 19 compatibility failures and production-relevant lint warnings without changing business behavior.

Purpose: This is the upgrade work itself. It keeps the same app, same Arabic workflow rules, and same role model while making the stack clean enough for production gates.
Output: Small compatibility commits with green lint, tests, typecheck, and build.
</objective>

<scope>
Allowed: fix build/type errors, async Next request API usage, middleware/proxy convention if it causes warnings or build risk, Supabase session compatibility, hook lint warnings, unused imports, and targeted tests for behavior at risk.

Not allowed: broad rewrites, visual redesign, replacing Supabase auth, changing RLS behavior, changing financial math, changing workflow order, or weakening role checks.
</scope>

<context>
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-01-SUMMARY.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-CONTEXT.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-RESEARCH.md
@.planning/phases/07-upgrade-next-js-stack-and-regression-test-the-app/07-PATTERNS.md
@AGENTS.md

<interfaces>
From `src/lib/supabase/server.ts`:
```ts
export async function createServerClient()
```

From `src/middleware.ts`:
```ts
export async function middleware(request: NextRequest)
```

From `src/lib/services/workflow.service.ts`:
```ts
async checkDependency(clientId: string): Promise<WorkflowDependencyStatus>
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Fix build and type compatibility blockers</name>
  <files>src/middleware.ts, src/lib/supabase/server.ts, src/lib/supabase/proxy.ts, src/app/api/**/route.ts, src/**/*.test.ts</files>
  <behavior>
    - Protected routes redirect unauthenticated users to `/login`.
    - Employees cannot access admin-only routes.
    - API routes keep returning JSON errors with correct status codes.
  </behavior>
  <action>Use the Plan 01 build/type errors as the work list. Fix the smallest files needed. For Next request APIs, keep async patterns like `await cookies()` and generated route types. If the build warns about deprecated `middleware.ts`, run the official Next migration approach or manually create the equivalent `proxy.ts` only if the app keeps the same route-guard behavior. This implements D-04, D-05, and D-06.</action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>Typecheck and build pass with no business-rule changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clean production-relevant React hook warnings</name>
  <files>src/hooks/useClients.ts, src/hooks/useDashboardAnalytics.ts, src/hooks/useDocuments.ts, src/hooks/useEmployees.ts, src/hooks/useFinancialDashboard.ts, src/hooks/useFinancials.ts, src/hooks/useWorkflows.ts, src/**/*.test.ts</files>
  <behavior>
    - Hooks keep the same returned fields: data, loading, error, and action functions.
    - Fetch actions still set loading and error states correctly.
    - Workflow and finance hooks do not bypass services or repositories.
  </behavior>
  <action>Fix React 19 hook warnings locally. Prefer stable callbacks, derived values, and dependency cleanup over disabling rules. Do not suppress lint globally. If a warning is intentionally accepted, document the exact reason in `07-02-SUMMARY.md`.</action>
  <verify>
    <automated>pnpm lint</automated>
    <automated>pnpm test</automated>
  </verify>
  <done>`pnpm lint` exits 0. Remaining warnings, if any, are documented and not about auth, role gates, workflows, finance, or shared hooks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Protect role and workflow dependency behavior</name>
  <files>src/lib/auth/**/*.ts, src/lib/services/workflow.service.ts, src/lib/services/workflow.service.test.ts, src/**/*.test.ts</files>
  <behavior>
    - Admin can access admin routes and employee management.
    - Employee cannot access admin-only routes.
    - Excavation Permit creation is blocked until Device License is completed.
  </behavior>
  <action>Add or update targeted Vitest coverage only where compatibility edits touched auth, permissions, or workflow dependency paths. Keep `WorkflowService.checkDependency` as the source of truth for Device License before Excavation Permit per D-06.</action>
  <verify>
    <automated>pnpm test</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>All production gates pass. Summary states which warnings were fixed or intentionally accepted.</done>
</task>
</tasks>

<risk_notes>
- Fixing hook warnings can change fetch timing. Keep tests focused on observable state and returned contracts.
- Auth fixes can look correct in unit tests but fail in the browser because cookies/storage are different. Browser Use proof happens in Plan 04.
- If renaming middleware to proxy, the matcher and cookie forwarding must stay equivalent.
</risk_notes>

<acceptance_criteria>
- `pnpm lint`, `pnpm test`, `pnpm typecheck`, and `pnpm build` pass after every edit round before committing.
- Arabic RTL root stays in `src/app/layout.tsx` as `<html lang="ar" dir="rtl">`.
- Role gates, RLS expectations, financial calculations, and workflow dependency behavior are unchanged.
- Summary lists changed files, tests run, and any accepted warnings.
</acceptance_criteria>

