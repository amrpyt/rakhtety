---
phase: 05
plan: 02
type: implementation
wave: 2
depends_on:
  - 05-01
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - EMP-03
files_modified:
  - src/lib/services/dashboard.service.ts
  - src/hooks/useDashboardAnalytics.ts
must_haves:
  truths:
    - "D-01: Dashboard uses live service data, not hardcoded arrays"
    - "D-04: Dashboard-facing data supports Arabic labels and EGP formatting in UI"
    - "D-11: Bottleneck data includes employee name when available"
  artifacts:
    - path: "src/lib/services/dashboard.service.ts"
      provides: "Live dashboard analytics service"
    - path: "src/hooks/useDashboardAnalytics.ts"
      provides: "React hook for dashboard analytics"
---

<objective>
Build the live dashboard analytics service and React hook.

Purpose: The dashboard needs one trusted data source for KPIs, bottlenecks, recent activity, and workload.
Output: Service plus hook.
</objective>

<tasks>
<task type="auto">
  <name>Create dashboard analytics service</name>
  <files>src/lib/services/dashboard.service.ts</files>
  <action>
    Load workflows, steps, clients, employees, and financial dashboard summary.
    Return one `DashboardAnalyticsSummary` object.
  </action>
  <acceptance_criteria>
    - Service exports `dashboardService`
    - Service calculates active files
    - Service calculates completed this month
    - Service includes pending debt from `financialService`
    - Service includes bottlenecks with client, step, employee, and stuck days
    - Service includes employee workload summaries
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create dashboard analytics hook</name>
  <files>src/hooks/useDashboardAnalytics.ts</files>
  <action>
    Add loading, error, refresh, and summary state around `dashboardService.getSummary`.
  </action>
  <acceptance_criteria>
    - Hook exports `useDashboardAnalytics`
    - Hook exposes `summary`
    - Hook exposes `loading`
    - Hook exposes `error`
    - Hook refreshes on mount
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `pnpm typecheck`
- Run `pnpm build`
</verification>

<success_criteria>
1. Dashboard data comes from services.
2. Hook exposes live dashboard summary.
3. No hardcoded dashboard service data is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-analytics/05-02-SUMMARY.md`
</output>
