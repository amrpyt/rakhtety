---
phase: 05
plan: 03
type: implementation
wave: 3
depends_on:
  - 05-02
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - EMP-03
files_modified:
  - src/components/dashboard/BottleneckList.tsx
  - src/components/dashboard/EmployeeWorkloadList.tsx
  - src/app/(dashboard)/dashboard/page.tsx
must_haves:
  truths:
    - "D-02: Dashboard keeps existing compact KPI card/grid style"
    - "D-03: Dashboard shows active files, completed this month, pending debt, and bottlenecks"
    - "D-08: Bottleneck list shows oldest stuck items first"
    - "D-11: Bottleneck rows show employee ownership"
  artifacts:
    - path: "src/components/dashboard/BottleneckList.tsx"
      provides: "Bottleneck list UI"
    - path: "src/components/dashboard/EmployeeWorkloadList.tsx"
      provides: "Employee workload UI"
---

<objective>
Replace hardcoded dashboard content with live dashboard analytics UI.

Purpose: Managers need to see real operational numbers and bottlenecks at a glance.
Output: Dashboard page wired to live summary plus bottleneck/workload lists.
</objective>

<tasks>
<task type="auto">
  <name>Create bottleneck and workload components</name>
  <files>src/components/dashboard/BottleneckList.tsx, src/components/dashboard/EmployeeWorkloadList.tsx</files>
  <action>
    Render bottlenecks with client, step, employee, stuck days, and empty state.
    Render employee workload summaries.
  </action>
  <acceptance_criteria>
    - Components use existing Card/Badge/Button patterns
    - Labels are Arabic
    - Bottleneck list shows client, step, employee, and days stuck
    - Workload list shows active workflow and step counts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire dashboard page to live analytics</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    Replace hardcoded KPI and activity arrays with `useDashboardAnalytics`.
    Keep existing financial KPI grid.
  </action>
  <acceptance_criteria>
    - Dashboard imports `useDashboardAnalytics`
    - KPI cards show live active files, completed this month, pending debt, and bottlenecks
    - Bottleneck section renders
    - Employee workload section renders
    - Build passes
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `pnpm typecheck`
- Run `pnpm build`
</verification>

<success_criteria>
1. Dashboard no longer relies on hardcoded operational KPI values.
2. Bottleneck view is visible.
3. Employee workload view is visible.
4. UI remains Arabic and compact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-analytics/05-03-SUMMARY.md`
</output>
