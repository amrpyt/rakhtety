---
phase: 05
plan: 04
type: implementation
wave: 4
depends_on:
  - 05-03
autonomous: true
requirements:
  - DASH-04
  - DASH-05
files_modified:
  - supabase/migrations/008_create_dashboard_alerts.sql
  - src/types/database.types.ts
  - src/lib/database/repositories/dashboard-alert.repository.ts
  - src/lib/services/dashboard-alert.service.ts
  - src/components/dashboard/BottleneckList.tsx
  - src/app/(dashboard)/dashboard/page.tsx
must_haves:
  truths:
    - "D-12: Alerts are persisted in-app records"
    - "D-13: External alert delivery is deferred"
    - "D-14: Alerts link back to workflow/step context when possible"
  artifacts:
    - path: "supabase/migrations/008_create_dashboard_alerts.sql"
      provides: "In-app alert persistence"
    - path: "src/lib/services/dashboard-alert.service.ts"
      provides: "Alert creation logic"
---

<objective>
Add in-app bottleneck alerts and wire the manager action.

Purpose: Managers need a simple way to notify the employee responsible for a stuck workflow step.
Output: Alert schema, repository/service, and dashboard button wiring.
</objective>

<tasks>
<task type="auto">
  <name>Create dashboard alerts schema and types</name>
  <files>supabase/migrations/008_create_dashboard_alerts.sql, src/types/database.types.ts</files>
  <action>
    Add `dashboard_alerts` table with workflow, step, recipient, creator, message, status, timestamps, indexes, and RLS policies with `TO authenticated`.
  </action>
  <acceptance_criteria>
    - Migration creates `dashboard_alerts`
    - Policies include `TO authenticated`
    - Alerts reference workflow and workflow step
    - TypeScript exports `DashboardAlert`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create alert repository and service</name>
  <files>src/lib/database/repositories/dashboard-alert.repository.ts, src/lib/services/dashboard-alert.service.ts</files>
  <action>
    Add create/list alert methods and a service method for bottleneck alerts.
  </action>
  <acceptance_criteria>
    - Repository exports `dashboardAlertRepository`
    - Service exports `dashboardAlertService`
    - Service validates recipient exists before creating alert
    - Alert message is Arabic
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire alert button into dashboard</name>
  <files>src/components/dashboard/BottleneckList.tsx, src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    Add an alert button for bottleneck rows with assigned employee ids and call the alert service from the dashboard page.
  </action>
  <acceptance_criteria>
    - Button is disabled when no employee is assigned
    - Successful alert shows visible feedback
    - Existing bottleneck list still renders
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `pnpm typecheck`
- Run `pnpm build`
- Verify migration policies include `TO authenticated`
</verification>

<success_criteria>
1. Manager can create an in-app alert for a bottleneck.
2. Alert records are secured by RLS.
3. External alert delivery stays out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-analytics/05-04-SUMMARY.md`
</output>
