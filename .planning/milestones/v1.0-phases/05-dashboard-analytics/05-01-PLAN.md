---
phase: 05
plan: 01
type: implementation
wave: 1
depends_on: []
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - EMP-03
files_modified:
  - src/types/database.types.ts
  - src/lib/services/dashboard-analytics.ts
  - src/lib/services/dashboard-analytics.test.ts
must_haves:
  truths:
    - "D-01: Dashboard numbers come from live workflow, step, employee, and financial data"
    - "D-03: KPI set includes active files, completed this month, pending debt, and bottlenecks"
    - "D-05: Bottleneck means an in_progress or blocked step older than 7 days"
    - "D-08: Bottlenecks sort oldest first"
    - "D-09: Employee workload includes assigned active workflows and assigned non-completed steps"
  artifacts:
    - path: "src/lib/services/dashboard-analytics.ts"
      provides: "Pure dashboard calculations"
    - path: "src/lib/services/dashboard-analytics.test.ts"
      provides: "Unit tests for bottleneck and KPI calculations"
---

<objective>
Create the pure calculation foundation for dashboard analytics.

Purpose: Keep dashboard math testable before connecting it to Supabase.
Output: Types, helper functions, and unit tests.
</objective>

<tasks>
<task type="tdd">
  <name>Add dashboard analytics types and helpers</name>
  <files>src/types/database.types.ts, src/lib/services/dashboard-analytics.ts</files>
  <action>
    Add dashboard summary, bottleneck, recent workflow, and employee workload types.
    Add pure functions for active file count, completed-this-month count, bottleneck detection, and workload counting.
  </action>
  <acceptance_criteria>
    - Exports `DashboardAnalyticsSummary`
    - Exports `DashboardBottleneck`
    - Exports `EmployeeWorkloadSummary`
    - Bottleneck helper only flags `in_progress` or `blocked` steps older than 7 days
    - Bottlenecks sort oldest first
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Add dashboard analytics tests</name>
  <files>src/lib/services/dashboard-analytics.test.ts</files>
  <action>
    Test active counts, monthly completion counts, bottleneck threshold behavior, and employee workload totals.
  </action>
  <acceptance_criteria>
    - Tests run without Supabase connection
    - Tests cover a fresh step that is not a bottleneck
    - Tests cover an old in-progress step that is a bottleneck
    - Tests cover employee workload counting
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `pnpm test -- src/lib/services/dashboard-analytics.test.ts`
- Run `pnpm typecheck`
</verification>

<success_criteria>
1. Dashboard calculation helpers exist.
2. Bottleneck rules match Phase 05 context.
3. Unit tests cover core calculations.
4. Types compile.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-analytics/05-01-SUMMARY.md`
</output>
