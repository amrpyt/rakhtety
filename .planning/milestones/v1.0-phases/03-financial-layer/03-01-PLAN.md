---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_create_financial_events.sql
  - src/types/database.types.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04

must_haves:
  truths:
    - "D-01: Payments are flexible records linked to a workflow with optional workflow_step_id allocation"
    - "D-02: Financial records are append-only events; corrections use refund or adjustment rows"
    - "D-05: Calculations use workflow_steps fee/profit snapshots, not latest workflow_step_configs"
    - "D-06: workflow_step_configs is the default price list; workflow_steps is the client-file cost snapshot; financial_events is the money movement ledger"
    - "D-11: Financial tables use RLS policies with TO authenticated"
    - "D-12: Database constraints protect money validity"
  artifacts:
    - path: "supabase/migrations/005_create_financial_events.sql"
      provides: "Financial event ledger table, constraints, indexes, RLS policies"
      contains: "CREATE TABLE IF NOT EXISTS public.financial_events"
    - path: "src/types/database.types.ts"
      provides: "Financial event and summary TypeScript types"
      contains: "FinancialEvent"
  key_links:
    - from: "financial_events.workflow_id"
      to: "workflows.id"
      via: "foreign key"
    - from: "financial_events.workflow_step_id"
      to: "workflow_steps.id"
      via: "nullable foreign key for step-specific payments"
---

<objective>
Create the database foundation for Phase 3 financial tracking.

Purpose: Money records need a safe ledger. A ledger means a table where every payment/refund/adjustment is saved as a permanent receipt row.
Output: Supabase migration plus TypeScript types for financial events and summaries.
</objective>

<execution_context>
@D:/Vibe Coding/02-Real PRojects with Clints/رخصتي/.codex/get-shit-done/workflows/execute-plan.md
@D:/Vibe Coding/02-Real PRojects with Clints/رخصتي/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-financial-layer/03-CONTEXT.md
@.planning/phases/03-financial-layer/03-RESEARCH.md
@src/types/database.types.ts
@supabase/schema.sql
@supabase/migrations/003_create_workflow_step_configs.sql

## Existing facts

- `workflow_steps` already has `fees` and `profit`.
- `workflow_step_configs` is the default price list only.
- Existing RLS policies must use `TO authenticated`.
- Currency default is EGP.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create financial events migration</name>
  <files>supabase/migrations/005_create_financial_events.sql</files>
  <read_first>
    - supabase/migrations/003_create_workflow_step_configs.sql
    - supabase/schema.sql
  </read_first>
  <action>
Create `supabase/migrations/005_create_financial_events.sql`.

Include:

1. `financial_event_type` enum with `payment`, `refund`, `adjustment`.
2. `financial_events` table with:
   - `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
   - `workflow_id UUID NOT NULL REFERENCES public.workflows(id) ON DELETE CASCADE`
   - `workflow_step_id UUID REFERENCES public.workflow_steps(id) ON DELETE SET NULL`
   - `client_id UUID NOT NULL REFERENCES public.clients(id) ON DELETE CASCADE`
   - `type financial_event_type NOT NULL`
   - `amount DECIMAL(12,2) NOT NULL`
   - `currency TEXT NOT NULL DEFAULT 'EGP'`
   - `payment_method TEXT`
   - `reference_number TEXT`
   - `notes TEXT`
   - `created_by UUID REFERENCES public.profiles(id)`
   - `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
3. Constraint that `amount > 0`.
4. Indexes for `workflow_id`, `workflow_step_id`, `client_id`, and `created_at`.
5. RLS enabled.
6. Policies with `TO authenticated`:
   - select: admin/manager or assigned workflow employee
   - insert: admin/manager or assigned workflow employee
   - update/delete: no broad policy; keep events append-only for MVP

Important: Do not create stored totals here. Totals are calculated later by the service.
</action>
  <verify>
    <automated>Select-String -Path supabase/migrations/005_create_financial_events.sql -Pattern "CREATE TABLE IF NOT EXISTS public.financial_events"</automated>
    <automated>Select-String -Path supabase/migrations/005_create_financial_events.sql -Pattern "TO authenticated"</automated>
  </verify>
  <done>
    Migration creates financial event ledger with constraints, indexes, and authenticated RLS policies.
  </done>
  <acceptance_criteria>
    - Migration contains `financial_events`
    - Migration contains `financial_event_type`
    - Migration contains `workflow_step_id UUID REFERENCES public.workflow_steps`
    - Migration contains `amount DECIMAL(12,2) NOT NULL`
    - Migration contains `CHECK (amount > 0)`
    - All RLS policies include `TO authenticated`
    - No cached totals table is introduced
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add financial TypeScript types</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - src/types/database.types.ts
  </read_first>
  <action>
Update `src/types/database.types.ts`.

Add:

```typescript
export type FinancialEventType = 'payment' | 'refund' | 'adjustment'

export interface FinancialEvent {
  id: string
  workflow_id: string
  workflow_step_id: string | null
  client_id: string
  type: FinancialEventType
  amount: number
  currency: string
  payment_method: string | null
  reference_number: string | null
  notes: string | null
  created_by: string | null
  created_at: string
}

export interface WorkflowFinancialSummary {
  workflow_id: string
  total_cost: number
  total_fees: number
  planned_profit: number
  total_paid: number
  realized_profit: number
  outstanding_debt: number
}

export interface FinancialDashboardSummary {
  total_fees_collected: number
  realized_profit: number
  outstanding_debt: number
}
```
</action>
  <verify>
    <automated>Select-String -Path src/types/database.types.ts -Pattern "FinancialEvent"</automated>
  </verify>
  <done>
    Financial event and summary types are available for repositories, services, hooks, and UI.
  </done>
  <acceptance_criteria>
    - File exports `FinancialEventType`
    - File exports `FinancialEvent`
    - File exports `WorkflowFinancialSummary`
    - File exports `FinancialDashboardSummary`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-to-database | Untrusted user input becomes financial records |
| employee-to-finance | Employee may only access workflows they are allowed to see |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tamper | financial_events.amount | mitigate | Database CHECK amount > 0 and service validation |
| T-03-02 | Information Disclosure | financial_events | mitigate | RLS policies with TO authenticated and workflow assignment checks |
| T-03-03 | Repudiation | financial_events history | mitigate | Append-only events with created_by and created_at |
</threat_model>

<verification>
- Run `pnpm typecheck`
- Verify migration contains `TO authenticated`
- Verify no direct cached totals table was added
</verification>

<success_criteria>
1. Financial event migration exists and is idempotent.
2. Financial event table links to workflows, clients, and optionally workflow steps.
3. Financial RLS policies include `TO authenticated`.
4. Types compile.

<output>
After completion, create `.planning/phases/03-financial-layer/03-01-SUMMARY.md`
</output>
