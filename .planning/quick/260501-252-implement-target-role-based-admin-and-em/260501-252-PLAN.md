---
mode: quick-full
id: 260501-252
title: implement target role-based admin and employee E2E flow permissions
type: execute
autonomous: true
files_modified:
  - src/config/auth.config.ts
  - src/lib/auth/permissions.ts
  - src/lib/auth/server-permissions.ts
  - src/middleware.ts
  - src/components/layout/Sidebar.tsx
  - src/app/(dashboard)/employees/page.tsx
  - src/app/(dashboard)/employees/[id]/page.tsx
  - src/app/(dashboard)/clients/[id]/page.tsx
  - src/app/api/employees/route.ts
  - src/app/api/clients/route.ts
  - src/app/api/clients/[id]/workflows/route.ts
  - src/app/api/workflow-documents/upload/route.ts
  - src/app/api/workflows/[id]/financial-summary/route.ts
  - src/app/api/clients/[id]/report/route.ts
  - src/lib/auth/permissions.test.ts
verification:
  - pnpm lint
  - pnpm test
  - pnpm typecheck
  - Browser Use E2E: admin/manager full access, employee allowed client/workflow/document/payment/report access, employee blocked from employees/settings/delete/emergency override
---

<objective>
Implement the target role behavior, not the current loose behavior.

Admin and manager can operate the full system: employees, bottleneck alerts, clients, workflows, documents, payments, and reports.
Employee can see all clients, update workflow steps, upload workflow documents, record payments, and read reports.
Employee cannot manage employees, roles, system settings, destructive deletes, or emergency override unless allowed later.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@src/config/auth.config.ts
@src/middleware.ts
@src/components/layout/Sidebar.tsx
@src/app/(dashboard)/employees/page.tsx
@src/app/(dashboard)/employees/[id]/page.tsx
@src/app/(dashboard)/clients/[id]/page.tsx
@src/app/api/employees/route.ts
@src/app/api/clients/route.ts
@src/app/api/clients/[id]/workflows/route.ts

Important project rules:
- Keep Arabic RTL setup unchanged. `dir="rtl"` belongs on the root html only.
- Do not weaken workflow order: excavation stays blocked until device license is complete.
- If any Supabase RLS policy is added or changed later, every policy must include `TO authenticated`.
- Use Browser Use for E2E checks.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create one shared role permission map</name>
  <files>src/config/auth.config.ts, src/lib/auth/permissions.ts, src/lib/auth/server-permissions.ts, src/lib/auth/permissions.test.ts</files>
  <behavior>
    - admin and manager return true for manageEmployees, manageRoles, manageSettings, manageClients, manageWorkflows, uploadWorkflowDocuments, recordPayments, readReports, readBottleneckAlerts, emergencyOverride, destructiveDelete.
    - employee returns true for readClients, updateWorkflowSteps, uploadWorkflowDocuments, recordPayments, readReports.
    - employee returns false for manageEmployees, manageRoles, manageSettings, destructiveDelete, emergencyOverride.
    - Unknown or missing role returns false for protected actions.
  </behavior>
  <action>
    Add a typed `PermissionAction` union and `rolePermissions` matrix in `src/lib/auth/permissions.ts`. Export `can(role, action)` and small helpers only if they keep route code simple. Keep this as the single truth, so UI and API do not drift.

    Add `src/lib/auth/server-permissions.ts` to read the current Supabase user plus profile role from a request-scoped server client, then expose `requirePermission(supabase, action)` returning `{ user, profile }` or throwing/returning a 401/403-safe result. Do not use `createAdminClient` to decide whether a user is allowed.

    Update `src/config/auth.config.ts` so protected route definitions can express role requirements. Keep login redirects unchanged unless route access needs a safer default.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/auth/permissions.test.ts</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
    Permission behavior is tested from the target decision list, and every later route/page can import one helper instead of inventing role checks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce permissions at API and server route boundaries</name>
  <files>src/middleware.ts, src/app/api/employees/route.ts, src/app/api/clients/route.ts, src/app/api/clients/[id]/workflows/route.ts, src/app/api/workflow-documents/upload/route.ts, src/app/api/workflows/[id]/financial-summary/route.ts, src/app/api/clients/[id]/report/route.ts</files>
  <behavior>
    - Unauthenticated requests still get 401 or login redirects.
    - Employee GET /api/clients succeeds.
    - Employee POST /api/clients succeeds only if target behavior allows normal client creation; if client creation is considered management in this codebase, require manageClients and document that choice in the summary.
    - Employee POST /api/clients/[id]/workflows can update allowed workflow flow only where target behavior allows it, but cannot use emergency override.
    - Employee POST /api/workflow-documents/upload succeeds for valid workflow/step pairs.
    - Employee payment/report reads succeed.
    - Employee GET/POST /api/employees returns 403.
    - Admin and manager requests succeed for the same operations.
  </behavior>
  <action>
    Use `requirePermission` from Task 1 at the top of protected route handlers before any admin-client operation. This matters because service-role Supabase calls bypass normal database security, so the route must check the person first.

    Harden `src/app/api/clients/[id]/workflows/route.ts`: keep the existing excavation dependency check, keep duplicate workflow protection, and add role checks before creating workflows/steps. Keep all client/workflow reads request-scoped where possible; only use `createAdminClient` after permission is proven and only for operations that truly need it.

    Add or update tests near existing route/service tests where practical. If route tests are not already easy to run, cover the permission helper with unit tests and verify APIs manually with Browser Use plus `fetch` from the browser session.
  </action>
  <verify>
    <automated>pnpm test</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
    API responses match the target role table: employee allowed work actions succeed, employee management/destructive/emergency actions return 403, admin/manager full operations still work.
  </done>
</task>

<task type="auto">
  <name>Task 3: Match dashboard UI to the same permissions and prove it in Browser Use</name>
  <files>src/components/layout/Sidebar.tsx, src/app/(dashboard)/employees/page.tsx, src/app/(dashboard)/employees/[id]/page.tsx, src/app/(dashboard)/clients/[id]/page.tsx</files>
  <action>
    Use the same permission map from Task 1 in the sidebar and dashboard pages. Hide `/employees` navigation and employee-management screens from employees. Keep clients, workflows, finance/payment, and report entry points visible for employees.

    On `src/app/(dashboard)/clients/[id]/page.tsx`, hide or disable emergency override controls for employees while keeping normal step update, document upload, payment recording, and PDF report access available. If the emergency button lives in a child component, pass an explicit permission prop from this page instead of rechecking loosely inside the child.

    Add a small forbidden state for direct employee visits to `/employees` or `/employees/[id]`: employees should see a clear 403-style message or be redirected to `/dashboard`; admin/manager should see the normal employee UI.
  </action>
  <verify>
    <automated>pnpm lint</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>Browser Use E2E: start `pnpm dev`, login as admin/manager, confirm sidebar shows employees and admin can open employees, clients, workflow, payments, reports. Then login as employee, confirm employee nav hides employees/settings, `/employees` is blocked, `/clients` opens, client detail can update a normal step, upload a workflow document, record a payment, open report, and cannot see/use emergency override or destructive delete.</automated>
  </verify>
  <done>
    Browser behavior matches API behavior. Employees get the work tools they need and lose management/destructive tools. Admin/manager keep full access.
  </done>
</task>

</tasks>

<threat_model>
| Threat | Risk | Mitigation |
|---|---|---|
| Employee calls hidden admin endpoints directly | UI-only hiding is easy to bypass | Task 2 checks permissions in API routes before work happens |
| Service-role Supabase bypasses RLS | Admin client can ignore database security | Task 2 proves permission before `createAdminClient` use |
| UI and API permission drift | Button says no but API says yes, or the reverse | Task 1 creates one shared permission map |
| Emergency override misuse | Employee completes blocked work without approval | Task 2 blocks API action and Task 3 hides UI control |
</threat_model>

<success_criteria>
- `pnpm lint`, `pnpm test`, and `pnpm typecheck` pass.
- Browser Use proves admin/manager full operations still work.
- Browser Use proves employee can see all clients, update workflow steps, upload workflow documents, record payments, and read reports.
- Browser Use proves employee cannot manage employees, roles, settings, destructive deletes, or emergency override.
</success_criteria>
